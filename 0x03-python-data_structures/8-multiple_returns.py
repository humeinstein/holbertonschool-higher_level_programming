#!/usr/bin/python3
def multiple_returns(sentence):
    total = len(sentence)
    if total > 0:
        hee = sentence[0]
    else:
        return None
    heehee = total, hee
    return(heehee)